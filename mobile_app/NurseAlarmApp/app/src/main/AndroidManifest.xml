<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NurseAlarmApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Splash Screen as Launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.NurseAlarmApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.NurseAlarmApp" />

        <activity
            android:name=".PatientDetailActivity"
            android:label="Patient Details"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".GeneralPatientDetailActivity"
            android:label="General Ward Patient"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".CameraStreamActivity"
            android:label="Live Camera Feed"
            android:screenOrientation="landscape"
            android:parentActivityName=".PatientDetailActivity" />

        <activity
            android:name=".GeneralWardActivity"
            android:label="General Ward - Nurse Mode"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".CriticalWardActivity"
            android:label="Critical Ward - Alert Monitor"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".CriticalPatientDetailActivity"
            android:label="Critical Ward - Patient"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".NetworkSettingsActivity"
            android:label="Network Settings"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>